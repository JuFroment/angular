import { Component, Input } from '@angular/core';
import { CarService } from './services/car/car.service';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {



  // finalTable!: Array<any>;
  // value!: Array<any>;

  // constructor() {
  // this.rows = (prompt("Combien de lignes souhaitez-vous ?"))
  // console.log(typeof(this.rows));

  // this.cols = (prompt("Combien de colonnes souhaitez-vous ?"))
  // parseInt(this.cols);
  // console.log(typeof(this.cols));

}

// addNewItem(newItem: Array<any>) {
//   this.finalTable.push(newItem);
//   console.log(this.finalTable)
// }

